# GitHub Pages deploy disabled — HearWise static site is deployed on Vercel.
# See vercel.json and https://vercel.com/docs
name: Deploy public HearWise app (disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy via Vercel instead. Connect the repo at https://vercel.com/new"
